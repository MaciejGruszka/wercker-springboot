box: maven:latest
build:
  steps:
    - xenoterracide/maven:
        goals: test
deploy:
  steps:
    # Deploy to Oracle ACCS 1455
    - peternagy/oracle-accs-deploy@0.0.1:
        opc_user: $OPC_USER
        opc_password: $OPC_PASSWORD
        region: em
        domain: $OPC_DOMAIN
        application_name: werckerapp1
        file: springbootdemo-0.0.1.war
        application_type: java
